import { describe, it } from "@effect/vitest";

describe(".gitignore", () => {
  it("should be the same everywhere", () => {
    // TODO: check templates directory for .gitignore and make sure they are all similar
  });
});
