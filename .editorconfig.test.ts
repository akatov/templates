import { describe, it } from "@effect/vitest";

describe('.editorconfig', () => {
  it('should be the same everywhere', () => {
    // TODO: check templates directory for .editorconfig and make sure they are all the same
  })
})
