import { describe, it } from "@effect/vitest";

describe('prettier.config', () => {
  it('should be the same everywhere', () => {
    // TODO: check templates directory for prettier.config.mjs and make sure they are all the same
  })
})
