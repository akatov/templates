import { describe, it } from "@effect/vitest";

describe('vitest.config', () => {
  it('should be the same everywhere', () => {
    // TODO: check templates directory for vitest.config.mjs and make sure they are all similar
  })
})
